# dat-format-expert — memory index

One line per memory: `- [Title](file.md) — hook`. Lessons land here when a format/interop
correction is reusable; durable *empirical* findings belong in `docs/knowledge-base/` instead
(dated), with at most a pointer here. No entries yet.
