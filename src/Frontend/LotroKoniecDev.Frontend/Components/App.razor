<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <base href="/"/>
    <link rel="stylesheet" href="@Assets["fonts.css"]"/>
    <link rel="stylesheet" href="@Assets["layout.css"]"/>
    @* The Blazor import-map component is deliberately absent (#670): it emits an inline script,
       which our script-src 'self' policy blocks. Nothing here needs it, because a Static SSR app
       never resolves a module specifier. Bring it back only together with a CSP nonce. *@
    <link rel="icon" href="data:,"/>
    <HeadOutlet/>
</head>

<body>
<Routes/>
<script src="@Assets["_framework/blazor.web.js"]"></script>
</body>

</html>
